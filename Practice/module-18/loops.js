// while loop
var num = 0;
while (num < 7) {
    // console.log(num);
    num++;
}

// sorting even numbers from 0 to 100
var numbers = 0;
while (numbers < 100) {
    // console.log(numbers);
    numbers += 2;
}

// sorting odd numbers from 0 to 100
var odds = 1;
while (odds < 100) {
    // console.log(odds);
    odds += 2;
}

// for loop
for (var num = 0; num < 9; num++) {
    // console.log(num);
}

// sorting even numbers from 0 to 100 using for loop
for (evenNums = 0; evenNums < 100; evenNums += 2) {
    // console.log(evenNums);
}

// sorting odd numbers for 0 to 100 using for loop
for (oddNums = 1; oddNums < 100; oddNums += 2) {
    // console.log(oddNums);
}

// simplified for loop
for (var i = 1; i < 100; i++) {
    // console.log(i += 2);
}

// while loop in reverse 
var rollNo = 10;
while (rollNo >= 1) {
    // console.log(rollNo);
    rollNo--;
}

// for loop in reverse 
for (var i = 20; i >= 1; i--) {
    console.log(i);
}